# object-oriented — 나만의 클래스 설계실

- 저장소: https://github.com/trmoo/object-oriented
- 배포 주소(Pages): https://trmoo.github.io/object-oriented/
- Pages 는 `.github/workflows/deploy.yml` 이 푸시마다 `npm test` → `npm run build` 를 돌려
  `dist/` 를 올린다. **`dist/` 를 커밋하지 않는다.**
  `actions/configure-pages@v5` 의 `enablement: true` 를 써서 Pages 설정을 손으로 켤 필요가 없다.

## 폴더 규칙
- **이 폴더(`D:\project_AI\object-oriented\`)가 이 앱의 메인 저장 폴더다.**
  소스·문서·설정·`node_modules/`·`dist/` 등 모든 것을 이 폴더 안에 둔다. 깃 저장소의 루트도 이 폴더다.
- **루트(`D:\project_AI\`)에는 아무것도 설치하지 않는다.** npm 명령은 반드시 이 폴더로 이동한 뒤 실행한다.
  ```bash
  cd D:\project_AI\object-oriented
  npm install
  ```
  루트에서 실행해야 하면 `npm --prefix object-oriented install` 처럼 설치 위치를 강제한다.
- 공통 규칙의 원본은 루트 `D:\project_AI\CLAUDE.md` 다. 작업 전에 그 파일을 먼저 읽을 것.

## ① 앱의 목적과 대상 단원
- **목적**: 학생이 **자기만의 클래스를 직접 설계**하면서 클래스 이름·속성·생성자·접근자·설정자·
  메소드·인스턴스 생성·메소드 호출을 몸으로 익히게 한다. 수행평가 2차시(설계)와 3차시(실전)의
  준비 도구다.
- **대상**: 고등학교 1학년 「프로그래밍(파이썬)」 2학기 · 객체 지향 프로그래밍
- **성취기준**: `[12정03-08]` 객체를 구현하는 클래스와 인스턴스를 활용하여 프로그램을 작성한다.
  - 해설: 실생활 사례로 객체 지향의 기본 개념을 이해하고 필요성을 설명할 수 있어야 한다.
    클래스와 객체를 생성하고 문제 해결을 위한 프로그램 구현에 활용할 수 있어야 한다.
  - 출처: `01. 교육과정/고등학교교육과정_성취기준.md` 247·256행

## 참고한 원본 자료 (모두 교사 로컬 문서에만 있고 저장소에 올리지 않는다)
아래 요약만으로 앱 수정이 가능하도록 적어 둔다.

### 1) 교안 PDF 34쪽 — `object-oriented/객체 지향 프로그래밍.pdf`
텍스트가 살아 있는 PDF다. 다시 읽으려면:
```bash
python -c "import sys; sys.stdout.reconfigure(encoding='utf-8'); from pypdf import PdfReader; r=PdfReader('객체 지향 프로그래밍.pdf'); [print('=== PAGE',i+1,'==='); print(p.extract_text()) for i,p in enumerate(r.pages)]"
```
- 1~19쪽 **객체 지향 프로그래밍**
  - 2쪽 추상화(抽象化) — 목적에 맞는 속성만 뽑아 상위 개념으로 범주화
    ※ 교안 원본에는 `抽像化` 로 인쇄되어 있고 「抽(뽑을 추) + 像(모양 상) + 化(될 화)」로 풀어 놓았다.
      표준 표기는 `抽象化` 다. 앱 화면에는 한자를 쓰지 않으니 그대로 두어도 되지만,
      교안을 고칠 때 참고할 것.
  - 3쪽 프로그래밍이란 = 실세계의 제한된 모형을 만들고 모의 수행하는 일
  - 5~6쪽 **객체 = 상태(속성) + 동작(기능)**, 표: TV·자동차·터틀·문자열 → 앱 탭①의 분류 활동
  - 7~8쪽 클래스 = 설계도·찍어내는 틀 / 인스턴스 = 찍어낸 각각의 객체
  - 9~15쪽 클래스 정의 문법을 한 장씩 강조: `class` → 클래스명(첫 문자 대문자) →
    메소드 매개변수 `self` → `self.속성명` → `self` 변수 → 생성자 `__init__` → 인스턴스 생성
    → **앱 탭①의 「클래스 정의 해부하기」가 이 순서를 그대로 따른다**
  - 17~18쪽 **Dice 예제** (`random.randint(1,6)`, dice1·dice2 두 인스턴스)
- 20~34쪽 **정보 은닉과 특별한 메소드**
  - 25~26쪽 정보 은닉 = 속성을 밖에서 함부로 못 바꾸게 함. 표기법 `self.__속성명`
  - 27쪽 **정보 은닉의 필요성** — ① 내부 구조 변경이 쉬움 ② 값의 유효성 검증
    (나이에 음수, 시간에 25시를 넣으려 할 때 거부) → **앱의 「설정자 유효성 검사」 기능 근거**
  - 28쪽 정보 은닉 없이 구현한 예 — `p.age = -5` 가 그대로 통과 → 탭② 예제 + 탭④ 실험 버튼
  - 29~30쪽 접근자(getter)·설정자(setter), **Person 예제**
  - 31쪽 수행평가 안내
  - 32쪽 **BankAccount** / 33쪽 **Circle** / 34쪽 **Car(`__str__`)**
  - ⚠ 교안 자체의 오류 두 곳을 앱에서 「오류 찾기」 문제로 쓴다
    - 33쪽 `calcArea` 가 `return area` 를 빠뜨려 `None` 이 나온다
    - 34쪽 `__str__` 에 「문자열을 반환해야 함」이라고 손글씨 메모가 있다

### 2) 수행평가 유의사항 — `2학기 객체지향 프로그래밍 수행평가 유의사항.pdf`
교사 문서 폴더에 있다. **실제 경로는 `LOCAL-NOTES.md`(저장소에 올리지 않는 파일)에 적어 두었다.**
그 폴더에는 학생 개인정보 파일도 함께 있으니 필요한 문서만 열 것.
- 1·2차시 준비(0점) → 3차시 실전(20점). 문항: **「나만의 클래스를 만들어 객체를 생성하고 활용하기」**
- **평가요소 7가지** (앱이 자동으로 확인하는 바로 그 항목들, `src/data.js` 의 `RUBRIC_ITEMS`)
  1. 키워드(`class`)를 활용하여 클래스를 올바른 형태로 정의
  2. 생성자를 속성을 활용하여 정의 (**비공개 속성 1개 이상**, 생성자 1개)
  3. 접근자 1개 이상 (`return self.__속성명`)
  4. 설정자 1개 이상 (`self.__속성명 = 매개변수`)
  5. 생성자·접근자·설정자 **외** 메소드 1개 이상 (속성을 활용해야 함)
  6. 생성자를 호출해 객체를 변수에 저장 (`인스턴스명 = 클래스명()`)
  7. 객체에 대해 메소드 호출 1회 이상 (`인스턴스명.메소드명()`)
- **배점**: 6가지 이상 20 / 5가지 18 / 4가지 16 / 3가지 14 / 2가지 12 / 1가지 10 /
  0가지 기본점수 / 미제출·백지 8 / 미인정결시 7
  - ⚠ 0가지일 때의 점수는 원문 표에서 값이 비어 있어 확인되지 않았다.
    앱에서는 숫자를 추측하지 않고 **「기본점수」라고만 표시**한다. 확인되면 `SCORE_TABLE` 수정.
- 평가 환경: 컴퓨터실 IDLE, **제한시간 30분**, 인터넷·USB·메모 금지, 화면 녹화,
  **2차시에 제출한 설계지(도장 받은 것)만 참고 가능**
- 학습지 p.34~36 코드를 변형하는 것은 가능하지만 **클래스 이름을 그대로 쓰면 감점**

### 3) 수행평가 설계지 — `수행평가(20점)_객체지향 프로그래밍(설계지).pdf`
- 채우는 칸 5개: 클래스 이름 / 비공개 속성 / 메소드 / 객체 생성 / 객체를 활용한 메소드 호출
  → **앱 탭③의 구성이 이 5칸을 그대로 따른다** (설계지 인쇄 기능은 2026-07-30 에 없앴다)
- 클래스 예시 50개 목록이 실려 있다 (설계지에 '학생(Student)'이 두 번 적혀 있어
  앱에는 **49개**만 담았다 → `src/data.js` 의 `CLASS_IDEAS`)

### 4) 학습지·실습지 — `객체 지향 프로그래밍(학습지, 실습지).pdf` 3쪽
- 30~31쪽 개념 빈칸 → **앱 탭①의 빈칸 문제 10개** (`BLANKS`)
- 32쪽 실습지는 PIL 이미지 처리(구글 코랩 필요)라 앱에 넣지 않았다

## ② 기능 목록

탭 순서는 수업 흐름을 따른다 — **① 부품 익히기 → ② 예제 살펴보기 → ③ 내 클래스 설계 → ④ 실행**
(2026-07-29 재배치. 순서를 바꿀 때는 `index.html` 의 버튼·패널 순서, `src/main.js` 의 `TABS`,
각 `src/tabs/*.js` 머리말, README 의 「화면 안내」 표를 함께 고쳐야 한다.
앱 안의 안내 문구는 번호가 아니라 **탭 이름**으로 서로를 가리키므로 고칠 필요가 없다)

### 탭① 부품 조립 연습 (`src/tabs/assemble.js`)
1. **클래스 정의 해부하기** — 골격의 노란 부분을 누르면 설명 (교안 9~15쪽 순서)
2. **객체 = 상태 + 동작 나누기** — 낱말 23개를 상태/동작으로 분류, 정답 확인
3. **코드 조각 순서 맞추기** — 3문제, 틀리면 몇 번째 줄이 어긋났는지 알려 준다
   (클래스만 정의하면 출력이 없는 이유도 설명해 준다)
4. **개념 빈칸 채우기** — 10문제, 빈칸마다 인정 답이 여러 개, 확인·힌트·정답
   - 「힌트」를 누르면 설명과 함께 **초성**을 보여 준다 (`toChosung()`). 상태 → ㅅㅌ
   - 영문 답은 첫 글자만 남기고 가린다 (class → `c····`, `__init__` → `__i···__`)
   - 인정 답이 여러 개면 **한글이 든 답**으로 초성을 만든다 (2 / 두 / 둘 → ㄷ)

### 탭② 교안 예제 (`src/tabs/examples.js`)
- 6개: Dice / 정보 은닉 없을 때(Person) / 접근자·설정자(Person) / BankAccount / Circle / Car
- 각 예제를 실행하고, **그 예제가 평가요소 몇 개를 만족하는지**까지 보여 준다
  (교안 예제도 7개를 다 채우지는 못한다는 것을 알게 해 준다)
  - 평가요소 **7개를 모두 나열**하고 만족한 것은 체크된 상자(`☑`), 못 한 것은 빈 상자(`☐`)로 보인다.
    항목마다 왜 되고 왜 안 되는지(`detail`)를 함께 적어 준다.
    ← 전에는 못 채운 항목만 보여 주고 「1, 6번 만족」처럼 번호만 적어서
      그 번호가 무엇인지 알 수 없었다. (2026-07-29 수정)
- Circle·Car 에는 **오류 찾기 문제**가 붙어 있다 (Circle 은 「고친 코드로 실행」 버튼 포함)

### 탭③ 클래스 설계실 (`src/tabs/design.js`) — 핵심
왼쪽에서 설계지 5칸을 채우면 오른쪽에 파이썬 코드가 실시간으로 조립되고 평가요소가 자동 확인된다.
1. **클래스 이름** — 첫 문자 대문자·영문 규칙 실시간 안내, 예시 49개에서 고르기(검색 가능)
   - 예시를 고르면 속성·메소드·인스턴스·호출까지 한 번에 채워진다 (`designFromIdea()`)
   - 「그대로 내면 감점」 경고 문구를 함께 띄운다
2. **비공개 속성** — 이름·한글 뜻·자료형(int/float/str/bool/list)·초깃값,
   「생성자에서 값을 받는다 / 초깃값으로 고정한다」 선택, 접근자·설정자 생성 체크,
   **설정자 유효성 검사**(최솟값·범위·빈 문자열 금지) — 교안 27쪽 근거
3. **메소드** — 동작 템플릿 8종 중 선택
   `inc`(늘리기) `dec`(줄이기) `print`(출력) `calc`(계산해 return) `check`(조건 검사)
   `toggle`(참/거짓 뒤집기) `reset`(초깃값 복귀) `custom`(직접 쓰기, 들여쓰기 자동 보정)
   + `__str__` 추가 체크박스
4. **객체 생성·활용** — 인스턴스명, 생성자 인수, 호출할 메소드 목록(`print( )` 감싸기 선택)
5. **저장** — 실행 탭으로 보내기 / 코드 복사 / `.py` 저장 / 처음부터
   - **2026-07-30 에 「설계 `.json` 저장」·「설계 불러오기」·「설계지 인쇄」 세 기능을 없앴다.**
     (사용자 지시. 수업에서 쓰이지 않았다.) 되살리지 말 것.
     함께 지운 것 — `doPrint()`, `loadDesign()`, `index.html` 의 `#print-sheet`,
     `style.css` 의 인쇄용 설계지 스타일, `codegen.js` 의 `designSheet()` 와 그 테스트 1개.
     학생이 설계를 남기는 수단은 「코드 복사」와 「`.py` 파일로 저장」이다.
- 오른쪽: 색칠된 코드(평가요소 주석 토글) + 평가요소 7개 체크리스트 + 예상 점수 + 실수 안내

### 탭④ 실행해 보기 (`src/tabs/run.js`)
- 코드 편집창 (Tab = 스페이스 4칸, Ctrl+Enter 실행), `input()` 용 입력값 칸
- 출력창 / **오류가 난 줄을 집어서 보여 주기**
- **인스턴스 상자** — 인스턴스마다 속성값을 표로, 비공개 속성에 🔒 표시
- **메소드 호출 기록** — 호출한 메소드 · 받은 값 · **바뀐 속성(전 → 후)** · 돌려준 값
- **정보 은닉 실험** 버튼 두 개 — 비공개 속성 직접 꺼내기(AttributeError 재현) /
  공개 속성에 이상한 값 넣기(-999 가 그대로 들어감)
- 손으로 고친 코드도 평가요소를 다시 확인해 준다

## 구조
```
object-oriented/
├─ index.html          껍데기 (머리말·탭 버튼·패널 4개)
├─ src/
│  ├─ main.js          탭 연결, 설계 상태(design)를 한 군데에 보관
│  ├─ pymini.js        ★ 교실용 미니 파이썬 해석기 (tokenize → parse → run)
│  ├─ codegen.js       설계 → 파이썬 코드 조립, 예시→설계 변환
│  ├─ rubric.js        평가요소 7가지 자동 검사 (pymini 의 AST 를 읽는다)
│  ├─ data.js          클래스 예시 49개, 평가요소 원문, 교안 예제, 문제 데이터
│  ├─ ui.js            h()·코드 색칠·토스트·파일 저장(.py)
│  ├─ style.css        교실 TV 기준으로 크게 (기본 19px)
│  └─ tabs/            assemble.js · examples.js · design.js · run.js  ← 탭 순서와 같게
└─ test/               node 로 돌리는 자체 점검 (브라우저 없이)
```

### 미니 파이썬 해석기 (`src/pymini.js`) — 이 앱의 심장
왜 직접 만들었나: 교실 인터넷이 끊겨도 학생 코드가 그 자리에서 돌아가야 하고,
**비공개 속성 접근을 막는 장면을 눈으로 보여 줘야** 하기 때문이다. Pyodide(10MB, 인터넷 필요)는
교실 조건에 맞지 않아 쓰지 않았다.
- 지원: `class` `def` `__init__` `self` `return` `if/elif/else` `while` `for ~ in range()`
  `import random·math` `print(sep·end)` `input()` 산술·비교·논리 연산, f-string,
  문자열·리스트·딕셔너리의 흔한 메소드, `__str__`, 키워드 인수, 기본값 매개변수
- **이름 맹글링을 진짜로 구현했다.** 클래스 안의 `self.__speed` 는 `_Car__speed` 로 저장되고,
  클래스 밖의 `myCar.__speed` 는 `AttributeError` 가 난다 → 정보 은닉 수업의 핵심 장면
- 오류 메시지는 **한국어 도움말과 줄 번호**를 함께 준다 (`PyError.format()`)
- 안전장치: 실행 단계 40만 회 / 출력 3000줄 / 인스턴스 200개 / 재귀 깊이 60 넘으면 중단
- 교안 PDF에서 복사한 **스마트 따옴표(‘ ’ “ ”)를 자동으로 보통 따옴표로 바꿔** 준다
- **미지원 문법은 「무엇이 안 되는지」 이름을 대어 알려 준다** (`지원하지 않는 문법` 오류)
  - `class 상속` — `class Dog(Animal) :` 은 막고 「괄호를 지우고 `class Dog :` 로 쓰세요」라고 안내한다.
    ⚠ 전에는 상속을 조용히 무시했는데, 그러면 물려받은 메소드를 부를 때
    「Dog 클래스에 'speak' 가 있는지 확인해 보세요」라는 **엉뚱한 AttributeError** 가 나서
    학생이 원인을 못 찾았다. 그래서 막는 쪽으로 바꿨다. (`class Foo(object) :` 는 통과시킨다)
  - `try/except` `lambda` `with` `raise` `assert` `yield` `async/await` — 이름을 대어 안내한다.
    try/except 에는 「if 문으로 값을 미리 검사하세요」라는 대안까지 붙는다.
  - `turtle`·`time` 등 다른 모듈 — 「random 과 math 만 지원합니다」라고 안내한다.
  - 판정은 `Parser.checkUnsupported()`(문장 첫 낱말)와 `atom()`(lambda), `classDef()`(상속)에서 한다.
    메소드 이름에 들어간 경우(`withdraw`)는 막지 않는다. 이 경계는 테스트로 지켜 두었다.

### 평가요소 자동 검사 (`src/rubric.js`)
글자 맞추기가 아니라 **AST 를 읽는다.** 그래서 학생이 손으로 고친 코드도 그대로 채점된다.
판정 규칙에서 주의할 점:
- 접근자·설정자는 **비공개 속성(`__`)을 다룰 때만** 인정한다 (유의사항 Tip 이 그렇게 되어 있다).
  공개 속성으로 썼으면 「`self.value` → `self.__value` 로 바꾸세요」라고 고칠 방법을 알려 준다.
- 설정자는 **`= 매개변수` 로 곧바로 넣는 것만** 인정한다.
  `self.__speed = self.__speed + amount` 는 설정자가 아니라 일반 메소드(평가요소 5)다.
  ← 이걸 놓쳐서 예시 절반이 평가요소 5를 못 받던 버그가 있었다. 규칙을 바꿀 때 주의.
- 클래스가 여러 개면 **메소드가 가장 많은 클래스**를 기준으로 본다.
- 점수는 **예상 점수(참고용)**라고 반드시 함께 표시한다. 실제 채점은 교사가 한다.

## 실행 방법
| 명령 | 하는 일 |
|---|---|
| `npm install` | 의존성 설치 (Vite, vite-plugin-singlefile) |
| `npm start` | 개발 서버 + 브라우저 열기 → `http://localhost:5174` 가 곧 `index.html` |
| `npm run dev` | 위와 같되 브라우저를 열지 않는다 (Claude Code 미리보기는 이쪽) |
| `npm run build` | `dist/index.html` **한 파일**로 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm test` | 해석기·코드생성·평가요소 자동 점검 (브라우저 없이, 88개) |

- 개발 중에는 ES 모듈로 여러 파일을 나눠 쓴다. `npm run build` 는 이것을 **iife 로 묶고
  `type="module"` 을 떼어** 낸다. 그래서 `dist/index.html` 은 **더블클릭만으로 열린다.**
  - `vite.config.js` 의 `plainScript` 플러그인이 `closeBundle` 에서 이 일을 한다.
  - `type="module"` 이 남아 있으면 **빌드를 일부러 실패시킨다** (더블클릭이 막히는 것을 방지).
  - `src/main.js` 는 `DOMContentLoaded` 를 기다린 뒤 화면을 만든다.
    일반 스크립트는 지연 실행되지 않아 `<head>` 에서 바로 돌기 때문이다. **이 가드를 지우면 안 된다.**
- Claude Code 미리보기 설정은 이 폴더의 `.claude/launch.json` 에 두 개 있다.
  `object-oriented`(개발, 5174) / `object-oriented-dist`(빌드 결과, 5175)

## ③ 현재 개발 상태와 다음 할 일
- **상태: 탭 4개 모두 완성** (2026-07-29, 2026-07-30 일부 기능 정리). 자체 점검 88개 전부 통과.
- **검증 완료** (개발 서버와 빌드 결과물 양쪽에서 실제로 눌러 보며 확인)
  - 교안 예제 6개의 출력이 교안과 일치 —
    BankAccount `100 입금 / 10 출금 / 90`, Person `<class '__main__.Person'>` → `향이 17`,
    Circle `원의 넓이: None`(교안 오류 재현) → `return area` 넣으면 `314.1592653589793`,
    Car `__str__` → `100, red`, Dice 두 인스턴스가 서로 다른 값
  - **클래스 예시 49개를 전부 골라 봤을 때 모두 실행 성공 + 평가요소 7/7 + 경고 없음**
  - 손으로 입력하는 흐름(클래스명 → 속성 → `custom` 메소드 → 호출)도 7/7
  - 정보 은닉 실험: `myCar.__speed` → `AttributeError` + 「`getSpeed( )` 를 만들어 쓰세요」 안내
  - 메소드 호출 기록: `Car.setSpeed( )` → `__speed: 0 → 100`
  - 조립 문제 정답 판정, 빈칸 10문제, 오류 찾기 정답 판정
  - 빌드 결과 `dist/index.html` 한 파일 **약 130KB**, 외부 참조 0개, `type="module"` 0개, `<script>` 1개
    ※ 크기는 빌드 로그의 「kB」가 아니라 실제 파일에서 재야 맞다. 한글이 UTF-8 3바이트라 차이가 크다.
      `node -e "console.log(require('fs').statSync('dist/index.html').size)"`
  - `type="module"` 안전장치가 실제로 작동하는지 **정규식을 일부러 어긋나게 해 확인했다** →
    빌드가 「type="module" 이 남아 있습니다」로 실패한다. (고치기 전에는 조용히 통과했다)
- **2026-07-30 변경 (사용자 지시)**
  - 탭③ 5번 카드에서 「설계 `.json` 저장」·「설계 불러오기」·「설계지 인쇄」 세 버튼을 제거.
    딸린 코드까지 함께 정리했다 — `doPrint()`, `loadDesign()`, `ui.js` 의 `pickFile()`,
    `codegen.js` 의 `designSheet()`(+테스트 1개), `index.html` 의 `#print-sheet`,
    `style.css` 의 인쇄용 설계지 스타일. 남은 버튼은 실행해 보기 / 코드 복사 / .py 저장 / 처음부터.
  - 5번 카드 안내 문구에서 「수행평가(3차시)」의 **차시 표기를 삭제** → 「수행평가는 …」.
    차시 번호는 수업 운영에 따라 달라지므로 앱 화면에 넣지 않는다. (쪽수·성취기준 코드와 같은 이유)
  - 「📄 만들어진 파이썬 코드」 상자가 **자체 스크롤**되게 했다 (`.code-wrap`).
    코드가 길어지면 오른쪽 sticky 칼럼이 화면 밖으로 자라 평가요소가 안 보이던 문제를 막는다.
    `max-height: max(360px, min(58vh, 620px))` — vh 를 0 으로 계산하는 화면에서도 접히지 않게
    360px 하한을 두었다. 세로·가로 스크롤은 상자가 맡고 안쪽 `pre` 는 `overflow: visible` 이다.
  - 실측(1280×860): 짧은 코드 172px(스크롤 없음) / 긴 코드 499px 고정 + 세로 스크롤 /
    아주 긴 한 줄에서 상자만 가로 스크롤(페이지 폭 1265px 그대로) / 콘솔 오류 없음 / 자체 점검 88개 통과
  - 탭②(교안 예제) 왼쪽 맨 아래에 **「null」 이라는 글자가 찍히던 버그**를 잡았다.
    `bodyBox.append(…, ex.bug ? bugQuiz(ex, run) : null)` 처럼 조건부 요소를 **DOM 의
    `append()` 인수로 바로 준 것**이 원인. `append()` 는 Node 가 아닌 값을 글자로 바꿔 넣기 때문에
    오류 찾기 문제가 없는 예제 4개(Dice·Person 2개·BankAccount)에서 `null` 이 보였다.
    → `if (ex.bug) bodyBox.append(bugQuiz(ex, run));` 로 분리했다.
    **`h()` 의 자식 자리는 falsy 를 걸러 주지만(`ui.js` 의 `add()`), 원시 `append()` 는 걸러 주지 않는다.**
    조건부 요소를 넣을 때는 `h()` 의 자식으로 주거나 `if` 로 감쌀 것.
    (같은 패턴을 전수 검사했다 — `assemble.js:316`·`examples.js:105` 은 `h()` 안이라 안전하다)
- **작업 중 잡은 버그 (같은 실수를 반복하지 않도록 기록)**
  1. `print('x', end='')` 가 앞줄에 잘못 붙던 문제 → 출력 버퍼(`emit`/`flush`)로 바꿔 해결
  2. 빈 설계에서도 「12점」이 보이던 문제 → 클래스 이름이 비면 점수를 매기지 않도록 막았다
  3. 조사가 「속력은(는)」처럼 나오던 문제 → 받침을 보고 조사를 고르는 `jo()` 추가
  4. `self.__x = self.__x + n` 을 설정자로 오판 → 평가요소 5가 안 잡히던 버그 (위 규칙 참고)
  5. 문자열 속성에 `dec`(줄이기) 템플릿이 걸려 실행 오류 → 템플릿에 맞는 속성을 골라 주게 수정
  6. 빈칸 문제 9번이 답 3개를 「빈칸 3개」로 오해해 입력칸이 3개 생기던 문제 →
     `a: [[답, 대체답], …]` 형태로 자료 구조 변경
  7. 빌드에서 `type="module"` 을 떼자 `<head>` 스크립트가 body 보다 먼저 돌아 화면이 비던 문제
     → `DOMContentLoaded` 가드 추가
- **공개 배포 전 점검(2026-07-29)에서 추가로 잡은 것** — 5가지 관점으로 훑고 지적마다 반박 검증
  8. `vite.config.js` 의 `type="module"` 안전장치가 **정작 필요한 상황에서 실행되지 않던 문제.**
     `if (html === before) return;` 이 검사보다 먼저 있어서, 정규식이 하나도 안 맞으면
     (= Vite 가 태그 모양을 바꾼 바로 그 경우) 조용히 통과해 더블클릭이 막힌 결과물이 나갈 수 있었다.
     → 검사를 **치환 직후·early return 앞으로** 옮겼다. 이 순서를 되돌리지 말 것.
  9. `class Dog(Animal) :` 을 조용히 무시하던 문제 (위 「미지원 문법」 항목 참고).
     상속은 학생이 자연스럽게 시도하는 문법인데, 엉뚱한 AttributeError 로 이어져 가장 나빴다.
     → 상속·`try/except`·`lambda`·`with`·`raise` 등에 이름을 대어 안내하도록 고치고 테스트를 붙였다.
  10. 문서에 적힌 `dist` 크기가 실제와 17% 달랐던 문제 (111KB ↔ 실제 133,001바이트).
      UTF-8 한글이 3바이트인데 **문자 수를 바이트로 착각**해 잰 것이 원인. 위 계측 명령을 쓸 것.
  11. `CLAUDE.md` 에 교사 로컬 문서 폴더의 절대경로(학교명·개인 태그가 들어 있었다)가 있던 것
      → `LOCAL-NOTES.md`(gitignore)로 옮겼다. 커밋 전에 잡아서 깃 히스토리에는 남지 않았다.
      **공개 저장소에 올릴 파일에 로컬 경로·계정 정보를 적지 말 것.**
      (교안 16쪽에는 구글 클래스룸 계정과 초기 비밀번호가 있다. 어떤 문서로도 옮기지 말 것)
- **다음 할 일 후보**
  - 0가지 만족일 때의 실제 점수를 유의사항 원문에서 확인해 `SCORE_TABLE` 에 반영
    (`…유의사항.hwp` 원본에서 확인 가능 — 위치는 `LOCAL-NOTES.md`)
  - 속성 여러 개를 쓰는 메소드 템플릿 (예: 가로×세로 말고 임의 조합)
  - 교안 2쪽 「추상화」 활동(여러 객체에서 공통 속성만 뽑기) 추가
  - 학습지 32쪽 PIL 실습과 이어지는 안내 (코랩 링크 정도)
  - 학생 코드를 서로 비교해 보는 「친구 코드 읽기」 활동 (개인정보는 담지 않는 형태로)
  - 오류·안내 문구의 조사 다듬기. `pymini.js`·`rubric.js` 에 `은(는)`·`이(가)` 표기가 30곳쯤 있다.
    변수 이름이 들어가는 자리라 소프트웨어에서 흔히 쓰는 표기이지만,
    `codegen.js` 의 `jo()` 를 공용 모듈로 옮겨 쓰면 「속력은」·「배터리는」처럼 자연스럽게 만들 수 있다.
    (생성되는 파이썬 코드의 문구는 이미 `jo()` 로 처리되어 있다)

## 주의 사항
- **앱 화면에는 교안·학습지 쪽수와 성취기준 코드를 표시하지 않는다.** (2026-07-29 결정)
  쪽수는 교안을 고치면 어긋나고, 학생 화면에는 군더더기다. 자료의 출처는 이 문서에만 적어 둔다.
  다시 넣지 말 것. 지웠던 자리 — 머리말의 `[12정03-08]`, 교안 예제의 쪽수 줄(`ex-meta`),
  문법 해부 설명의 쪽수 줄(`part-info .page`), 각 안내 문구의 `(교안 ○쪽)`.
- **학생 개인정보를 수집·저장하지 않는다.** `localStorage`·`sessionStorage` 도 쓰지 않는다.
  그래서 창을 닫으면 설계가 사라진다. 대신 「코드 복사」·「`.py` 파일로 저장」 버튼과
  「창을 닫으면 사라진다」는 안내를 5번 카드에 두었다. **자동 저장을 넣지 말 것.**
- 화면 문구는 모두 한국어, 코드 주석도 한국어로 유지한다.
  생성되는 파이썬 코드의 한국어 문구는 `codegen.js` 의 `jo()` 로 조사를 맞춘다.
- 수행평가는 IDLE 에서 **직접 타이핑**한다. 앱이 코드를 만들어 주지만
  「손으로 쳐 보는 연습을 하라」는 안내를 반드시 남겨 둘 것 (5번 카드).
- 앱을 고친 뒤에는 `npm test` 를 돌리고, 이 파일의 「현재 개발 상태」를 갱신한다.
- 교안·수행평가 PDF 는 저장소에 올리지 않는다. 필요한 내용은 위 요약에 담아 두었다.
